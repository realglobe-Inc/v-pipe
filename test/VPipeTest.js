/**
 * Test for VPipe.
 * Runs with mocha.
 */
'use strict'

const VPipe = require('../lib/VPipe')
const { ok, equal } = require('assert')

describe('v-pipe', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    ok(VPipe)
  })
})

/* global describe, before, after, it */
